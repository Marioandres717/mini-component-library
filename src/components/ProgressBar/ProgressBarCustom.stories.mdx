import { Meta, Story } from '@storybook/addon-docs/blocks';
import ProgressBarCustom from './ProgressBarCustom';

<Meta
  title="ProgressBarCustom"
  component={ProgressBarCustom}
  argTypes={{
    value: {
      control: {
        type: 'number',
        min: 0,
        max: 100,
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
  }}
/>

export const Template = (args) => <ProgressBarCustom {...args} />;

# ProgressBarCustom

<Story name="Default" args={{ value: 50, size: 'medium' }}>
  {Template.bind({})}
</Story>
